# Evidenza — Preparazione dell'ambiente e metodo di lavoro

## Ambiente verificato

La postazione utilizza Windows con WSL 2 e una distribuzione Ubuntu supportata. I progetti sono conservati nel filesystem Linux e vengono aperti con Visual Studio Code tramite l'estensione WSL.

| Componente         | Versione o stato pubblicabile | Verifica eseguita                            |
| WSL                | 2.7.13.0 - WSL 2              | `wsl --list --verbose` e `wsl --version`     |
| Ubuntu             | 26.04 LTS                     | `/etc/os-release`                            |
| Visual Studio Code | 1.136.1                       | `code --version` e `code .`                  |
| Git in Ubuntu      | 2.53.0                        | `git --version`                              |
| Azure CLI          | 2.90.0                        | `az version`, `az login` e `az account show` |

## Workflow completato

Ho clonato il repository `corso-azure-devops`, ottenendo così accesso al materiale del corso. Successivamente ho creato il mio repository personale `azure-devops-lab`, nel quale ho predisposto le cartelle `evidenze` e `laboratori`. Dopo aver modificato il file `evidenze/UD01.md` e creato `laboratori/UD01/nota-operativa.md`, ho utilizzato `git status` per controllare quali file risultavano modificati o non ancora tracciati. Con `git add evidenze/UD01.md laboratori/UD01/nota-operativa.md` ho selezionato in modo esplicito i file da includere nel commit. Ho quindi utilizzato `git commit` per registrare le modifiche nel repository locale e, infine, `git push` per pubblicare il commit nel repository remoto su GitHub. Dopo il push ho verificato su GitHub che il commit e i file pubblicati fossero correttamente presenti nel repository personale.


## Verifica finale

Ultimo commit visibile su GitHub: Completa setup ed evidenza dell'UD01

```
Commit 85ea0ee Completa setup ed evidenza dell'UD01
```

Stato dell'invito al docente (`inviato` oppure `accettato`): accettato.


Problema incontrato o possibile: l'account Azure risultava autenticato, ma non era ancora associato a una sottoscrizione.

Prima verifica diagnostica: `az account show` / `az login`, che inizialmente segnalavano l'assenza di sottoscrizioni disponibili.
 
## Controllo sicurezza

- [ok] Non sono presenti password, token, chiavi o codici temporanei.
- [ok] Non sono presenti e-mail o nomi utente non necessari.
- [ok] Non sono presenti subscription ID o tenant ID.
- [ok] Gli screenshot eventuali sono stati controllati e ritagliati.
- [ok] Gli output riportati sono limitati alle informazioni utili.
